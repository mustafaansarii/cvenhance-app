spring:
  application:
    name: AUTH-SERVICE

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  datasource:
    url: jdbc:postgresql://postgres:5432/cvenhance?currentSchema=auth_schema
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    show-sql: ${JPA_SHOW_SQL:false}
    properties:
      hibernate:
        default_schema: ${DB_SCHEMA:public}
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: ${APP_TIMEZONE:Asia/Kolkata}

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:dummy-google-id}
            client-secret: ${GOOGLE_CLIENT_SECRET:dummy-google-secret}
            redirect-uri: ${OAUTH_REDIRECT_BASE:http://localhost:5173/careerhub}/login/oauth2/code/{registrationId}
            scope:
              - profile
              - email
          github:
            client-id: ${GITHUB_CLIENT_ID:dummy-github-id}
            client-secret: ${GITHUB_CLIENT_SECRET:dummy-github-secret}
            redirect-uri: ${OAUTH_REDIRECT_BASE:http://localhost:5173/careerhub}/login/oauth2/code/{registrationId}
            scope:
              - read:user
              - user:email

logging:
  level:
    root: ${LOG_LEVEL_ROOT:ERROR}
    com.cvenhance.auth: ${LOG_LEVEL_APP:TRACE}
    org.hibernate.SQL: OFF

server:
  port: 8081

google:
  clientId: ${GOOGLE_CLIENT_ID:}
  clientSecret: ${GOOGLE_CLIENT_SECRET:}

github:
  clientId: ${GITHUB_CLIENT_ID:}
  clientSecret: ${GITHUB_CLIENT_SECRET:}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

resend:
  api-key: ${RESEND_API_KEY:}

mail:
  from: ${MAIL_FROM:}
  support: ${MAIL_SUPPORT:support.cvenhance@gmail.com}

frontend:
  url: ${FRONTEND_URL:http://localhost:5173}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}

oauth:
  success-redirect: ${OAUTH_SUCCESS_REDIRECT:http://localhost:5173}
  failure-redirect: ${OAUTH_FAILURE_REDIRECT:http://localhost:5173/login}

auth:
  jwt:
    secret: ${AUTH_JWT_SECRET:your-256-bit-secret-key-must-be-at-least-32-characters-long}
    expiry-ms: ${AUTH_JWT_EXPIRY_MS:3600000}
  session:
    expiry-ms: ${AUTH_SESSION_EXPIRY_MS:2592000000}
  cookie:
    name: ${AUTH_COOKIE_NAME:ACCESS_TOKEN}
    secure: ${AUTH_COOKIE_SECURE:false}
    same-site: ${AUTH_COOKIE_SAME_SITE:Lax}
    path: ${AUTH_COOKIE_PATH:/}
